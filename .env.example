# Local development (host Python)
DATABASE_URL=sqlite:///./test.db

# Docker: DATABASE_URL is overridden by docker-compose.yml — no change needed here.
# The container uses: sqlite:////data/db/complaints.db (absolute path, 4 slashes)

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=https://api.openai.com/v1
